# R / RStudio
.Rproj.user
.Rhistory
.RData
.Ruserdata

# Local-only agent files
.claude/
.cursor/
AGENTS.md
CLAUDE.md

# macOS
.DS_Store

# Quarto cache, per-render figure bundles, notebook intermediates
.quarto/
*_files/
**/*.quarto_ipynb

# Unused site index — search is off in _quarto.yml, but Quarto still writes this
docs/search.json

# LaTeX intermediates from the lualatex CV pipeline
*.aux
*.log
*.out
*.toc
*.synctex.gz
*.fls
*.fdb_latexmk
*.bbl
*.blg
*.xdv

/.quarto/
